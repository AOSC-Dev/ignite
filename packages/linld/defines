# shellcheck shell=bash
VER=0.9.7
SRCS=(
    'tbl::https://busybox.net/~vda/linld/linld097devel.tar.bz2'
)
CHKSUMS=(
    'sha256::8bd4fc31bdfc6afda990846911f2bb6a3c6eba128df0d9cab27315fa6d0a1ff4'
)

PKGNAME=linld
BUILDDEP=(
    open-watcom
    nasm
    make
)

build() {
    abwarn "TODO: use the Open Watcom compiler to build linld instead of using the prebuilt binaries."
    # make
    install -Dvm755 DIST/LINLD097/LINLD.COM "$PKGDIR"/LINLD.COM
}
